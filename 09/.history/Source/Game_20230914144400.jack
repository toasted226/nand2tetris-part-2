class Game {
    field Player player;

    constructor Game new() {
        let player = Player.new(0, 0, 20);
        return this;
    }

    method void run() {
        var int key;
        var bool exit;
        return;
    }

    method void dispose() {
        do player.dispose();
        do Memory.deAlloc(this);
        return;
    }
}
